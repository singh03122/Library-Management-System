# Server port (Railway automatically binds to PORT env variable, or you can set it)
server.port=${PORT:8080}

# View resolver
spring.mvc.view.prefix=/WEB-INF/view/
spring.mvc.view.suffix=.jsp

# Database connection using Railway environment variables
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=WVQWaQSmFYhVmniQSTtUiUgdsizuiIof

# Hibernate settings (read-only or managed mode recommended)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
